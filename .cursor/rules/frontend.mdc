---
alwaysApply: false
---
# 前端（Vue3 + TypeScript）

## 规范约定（引用）

- 全局语法、命名、**JSDoc**、目录、错误与测试覆盖率：`.my-rules/规范/AI_Coding_Style.md`（若团队使用 `Cursor/规范/` 副本则同等效力）。
- **风格与格式**：以 `packages/frontend` 内 ESLint / Prettier 配置为准。
- 流程与红线：`rules/SpecCoding.mdc`。
- **提交信息**：遵循 Conventional Commits（与 Git 历史风格一致）。

## 技术栈（以 package.json 为唯一事实来源）

默认 Vue 3 **Composition API** + TypeScript + Vite；状态与路由以项目已选依赖（如 Pinia、Vue Router）为准。

## 必须遵守

- **类型**：导出函数/Composable 的入参与返回值显式类型；避免无必要的 `any`。
- **安全**：无密钥、Token、内网地址等硬编码；环境变量按项目既有模式读取。
- **结构**：按现有目录习惯拆分（views、components、store、utils 等）；单文件过长则拆 Composable/子组件。
- **样式**：优先 scoped 或 CSS Modules；避免污染全局；BEM 等项目若已有约定则跟随。

## 质量与性能

- **测试**：工具函数覆盖率与关键流程用例要求见 `AI_Coding_Style`；若 `rules/TEST.mdc` 或 CI 另有阈值则从更严者。
- **性能**：以测量为准；涉及性能验收时可对照 **LCP 小于 2.5s、CLS 小于 0.1、首屏加载时间小于 1500ms**（与业务基线冲突时以产品/监控为准）。

## 不需要做的事

- 不在每次回复套用长篇幅「交付报告 / 五级任务模板」；任务说明写清范围、验收标准即可。
- 不罗列 Jest/Vitest/Cypress 等工具的通用命令行说明。

## 归档（长版交付模板）

历史「开发交付报告 / 任务指令」等大模板见 `.my-rules/frontend.md`。

## 禁止

- Vue 2 Options API（除非维护遗留且任务明确仅限旧代码）。
- 为赶进度去掉错误处理、空值分支或安全默认值。
