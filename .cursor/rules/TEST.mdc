---
alwaysApply: false
---
# 测试

## 定位

基于**明确范围**做功能 / 接口 / 兼容 / 性能 / 回归；缺陷可复现、可分级、可闭环。

## 级别 P0–P3

- **P0**：核心不可用或数据严重错误，阻塞发布。
- **P1**：主路径受损或高风险逻辑错误。
- **P2**：非主路径缺陷。
- **P3**：文案、轻微视觉、体验类。

## 用例与缺陷（最小要求）

- 用例含：前置条件、步骤、**可观察**的预期；优先级与范围对齐产品。
- **覆盖目标**（功能测试基线）：**核心场景用例覆盖 100%**，非核心场景 **≥95%**，含正向、逆向、边界与异常（与发布门禁冲突时以项目规定为准）。
- 缺陷含：环境、步骤、实际 vs 期望、证据（截图/日志摘要）；避免「凭感觉」无法复现。
- 状态标记如需统一：✅ / ❌ / 🔴 / 🟡（与团队文档一致即可）。

## 报告最小结构

- 范围与环境、通过率摘要、**未通过项列表**（级别/影响/建议）、发布建议。
- 性能/兼容小节**仅**在任务范围内展开；指标阈值若 CI 或项目文档有定义则引用之。

## 执行注意

- 回归覆盖**修改点 + 合理关联面**；冒烟验证主路径是否通。
- 工具链（Postman、Jira、Lighthouse 等）按团队已有流程使用，本规则不写操作手册。

## 归档（长模板）

需与历史测试报告/清单长模板对齐时，参见 `.my-rules/TEST.md`。

## 禁止

- 为凑覆盖率编造不可执行用例。
- 级别含糊或「全局必须通过 100%」脱离上下文。
- 重复仓库内已有测试策略全文——改**链接**到 `rules/TEST.mdc` 以外的专文（若存在）。
