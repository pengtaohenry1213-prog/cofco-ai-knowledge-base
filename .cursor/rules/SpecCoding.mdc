---
alwaysApply: false
---
# Spec Coding（规范驱动开发）

## 模式与红线

- **模式**：规范驱动开发；禁止无规范的即兴开发。
- **原则**：需求/方案/接口或契约对齐后再编码；合并前自检与 CR；禁止为进度牺牲安全与可维护性。
- **红线**：无边界和验收则不开工；不擅自改契约；不硬编码密钥与敏感配置；异步路径须有错误处理。

## 推荐流程（可裁剪）

需求澄清 → 方案或接口评审 → UI/交互对齐 → 任务拆分 → 编码 → CR → 自测 → 联调/验收 → 复盘。

## 规范源与优先级（引用，不复制全文）

**本仓库内路径**（优先）：

- 全局编码：`.my-rules/规范/AI_Coding_Style.md`
- 规范守护者说明：`.my-rules/规范/Specification_Guardian.md`（若存在）

**团队若另有副本**：`Cursor/规范/` 下同名文件与上列**同等效力**，以实际存在的路径为准。

**角色补充**：`rules/*.mdc`（与 `.cursor/rules` 同步时应一致）。

**冲突处理**：`AI_Coding_Style` → 当前角色 `rules/*.mdc` → 本文件流程。

## 代码合入最低要求

- 通过仓库已配置的 ESLint / Prettier / TypeScript 等检查。
- 结构清晰；契约、类型与错误处理与项目约定一致。

## 长模板与历史全文

完整 PRD/IXD/测试报告等大模板曾见于 `.my-rules/cursorroles*.md`、`.my-rules/cursorrules*.md`；日常以各 `rules/*.mdc` **最小结构**为准，需要逐字段对齐时再到 `.my-rules` 查阅归档，避免规则与归档双份失真。

## 规则维护原则

- 单文件保持简短；格式细则以 **linter + AI_Coding_Style** 为准。
- **引用路径**替代大段复制；通识命令不写入规则。

## 关系示意

```mermaid
flowchart LR
  A[SpecCoding] --> B[AI_Coding_Style]
  A --> C[rules 角色]
  C --> B
```
